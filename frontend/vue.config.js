// import Vue from 'vue';

// Vue.config.warnHandler = function (msg, vm, trace) {
//   // Ignore $attrs and $listeners warnings
//   if (msg.includes('$attrs is readonly') || msg.includes('$listeners is readonly')) {
//     return;
//   }

//   // Otherwise, log the warning to the console
//   console.warn(msg, vm, trace);
// };

// Vue.config.silent = true;